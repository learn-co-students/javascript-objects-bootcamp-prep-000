var playlist = {key:'value'}

function updatePlaylist(playlist, key, value) {
  var playlist = {["Phil Ochs"]:"Here's to the State of Mississippi"}
  return playlist
}

function removeFromPlaylist(playist, key) {
	var playlist = {key:'value'}
	delete playlist.key
	return playlist
}
