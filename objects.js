var playlist = {"Jackson":"Apidou"};
function updatePlaylist(playlist,name,title) {
  playlist[artist] = title;
  return playlist();
}
function removeFromPlaylist(object, name) {
  delete.playlist[artist];
  return playlist;
}
