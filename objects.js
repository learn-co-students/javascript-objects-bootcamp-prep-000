var playlist = {
  artistName: songTitle,
}

function updatePlaylist(playlist, artistName, songTitle) {
  playlist[artist name] = songTitle

  return playlist
}

function removeFromPlaylist(playlist, artistName) {
  delete playlist.artistName
}
