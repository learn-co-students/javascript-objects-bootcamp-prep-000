var playlist = {johnnyFlynn: "The Box"};

function updatePlaylist(ob, artistName, songTitle) {
  playlist.artistName = "songtitle";
  return playlist;
}

updatePlaylist(playlist, rickyMartin, "song");
